import { Layout } from 'antd';

export const Settings = () => {
  return (
    <Layout.Sider style={{ position: 'fixed', right: 0, marginRight: '-100%' }}>
      Settings bar
    </Layout.Sider>
  );
};

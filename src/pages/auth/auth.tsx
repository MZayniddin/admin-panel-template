import { AuthCard, AuthCardTitle, AuthSection } from './style';

export const Auth = () => {
  return (
    <AuthSection>
      <AuthCard>
        <AuthCardTitle>sign in</AuthCardTitle>
      </AuthCard>
    </AuthSection>
  );
};
